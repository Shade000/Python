def count_vowels(str):
    s = list(str)
    a = list('aeiouy')
    count = 0
    for i in s:
        for x in a:
            if i == x:
                count += 1
    return count
    
count_vowels('abcd')
